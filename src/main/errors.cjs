const {CODES}=require('./diagnostics.cjs');
const HELP={OLLAMA_NOT_INSTALLED:'Install Ollama, then test again.',OLLAMA_NOT_RUNNING:'Ollama is not running. Start Ollama and retry.',MODEL_NOT_INSTALLED:'Download the selected model before continuing.',MODEL_HEALTH_FAILED:'The model could not answer a short test. Try a smaller model or restart Ollama.',GATEWAY_UNREACHABLE:'Common cannot reach the gateway. Check your connection; Common will retry automatically.',GATEWAY_AUTH_FAILED:'The gateway rejected your credentials. Check the saved contributor token.',GATEWAY_MALFORMED:'The gateway returned an unexpected response. Try again or report a problem.',TUNNEL_RATE_LIMITED:'Cloudflare is rate-limiting new connections. Common will wait ten minutes before retrying.',TUNNEL_START_FAILED:'The connection helper could not start. Install the helper and retry.',TUNNEL_TIMEOUT:'The connection helper timed out. Common will retry automatically.',REGISTRATION_FAILED:'Common could not register this computer. Retry or report a problem.',REGISTRATION_EXPIRED:'The node registration has expired. Common will register again.',WORKER_UNREACHABLE:'Your public connection exists, but the gateway cannot reach the worker. Common will retry automatically.',INFERENCE_TIMEOUT:'The answer took too long. Try a smaller model or retry.',INFERENCE_FAILED:'The model could not finish this answer. Retry or report a problem.',INFERENCE_CANCELLED:'Answer stopped.',UPDATE_FAILED:'The update could not be verified or downloaded. Keep using this version and retry later.',SECURE_STORAGE_UNAVAILABLE:'The operating system credential store is unavailable. Unlock it and restart Common.',REPORT_FAILED:'The report was not submitted. Retry later or export diagnostics.',APP_CRASHED:'Common closed unexpectedly. A sanitised diagnostic report is available.',UNKNOWN:'This action failed. Test your setup or report a problem.'};
function failure(code,cause){const e=new Error(HELP[code]||HELP.UNKNOWN,{cause});e.code=code;e.status=cause?.status;return e;}
function classify(e,fallback='UNKNOWN'){if(CODES.has(e?.code))return e.code;if(e?.code==='registration_expired')return 'REGISTRATION_EXPIRED';if(['worker_auth','registration_missing','auth_unknown'].includes(e?.code)||[401,403].includes(e?.status))return 'GATEWAY_AUTH_FAILED';if(e?.name==='TimeoutError'||e?.cause?.code==='ETIMEDOUT')return fallback==='INFERENCE_FAILED'?'INFERENCE_TIMEOUT':fallback;return fallback;}
function errorFields(e,fallback){return {code:classify(e,fallback),httpStatus:e?.status||null,causeType:e?.cause?.name||e?.name,causeCode:e?.cause?.code};}
module.exports={HELP,failure,classify,errorFields};
